# Production frontend build: point at the Express backend origin.
VITE_API_URL=https://mediumpurple-chicken-145151.hostingersite.com

# Local Vite: leave empty (or unset) so /api is proxied to localhost:5001
# VITE_API_URL=
