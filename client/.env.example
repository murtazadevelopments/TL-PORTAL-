# Build-time only (Vite). Must be the backend origin (no trailing /api).
# API paths in code are /api/auth/..., /api/users/..., etc.
VITE_API_URL=https://mediumpurple-chicken-145151.hostingersite.com
