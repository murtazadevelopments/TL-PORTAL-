# Build-time only (Vite). Production example:
# VITE_API_URL=https://mediumpurple-chicken-145151.hostingersite.com/api
#
# Local dev: leave unset so requests use /api (Vite proxy → localhost:5001)
VITE_API_URL=
